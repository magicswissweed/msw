name: Verify

on:
  push:
    paths-ignore:
      - '**.md'
      - '.git/'
      - '.idea'
    pull_request:
      types:
        - opened
        - reopened
        - synchronize
        - ready_for_review

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT }}
  GAR_LOCATION: europe-west6
  GAR_REPOSITORY: msw-backend
  IMAGE: msw-backend

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 21

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Docker configuration
        run: gcloud auth print-access-token | docker login -u oauth2accesstoken --password-stdin https://$GAR_LOCATION-docker.pkg.dev

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2

      - name: Start Database
        run: docker compose --project-name msw -f docker-compose.yml up -d

      - name: 🔨 Build
        run: |
          chmod +x ./gradlew
          ./gradlew build -x test

      - name: 🧪 Test
        run: |
          chmod +x ./gradlew
          ./gradlew test

      - name: 🖨️ Copy Jar File
        run: mv backend/build/libs/$(ls backend/build/libs | grep -v plain) backend/app.jar

      - name: 🐳 Build Docker Image
        run: |
          docker build \
            --tag "$GAR_LOCATION-docker.pkg.dev/$PROJECT_ID/$GAR_REPOSITORY/$GITHUB_SHA" \
            --build-arg JAR_FILE=app.jar \
            backend

      - name: 🚀 Push Image to GCP Artifact Registry
        if: github.ref == 'refs/heads/master'
        run: |
          docker push "$GAR_LOCATION-docker.pkg.dev/$PROJECT_ID/$GAR_REPOSITORY/$GITHUB_SHA"
