###
# @name Get Current Sample
#
GET {{base-path}}/sample/current/{{stationId}}
Authorization: Bearer {{auth_token}}

###
# @name Get Samples of last 40 days
# make sure the stationId does not have a forecast (-> this probably means, there is a last40Days available)
#
GET {{base-path}}/sample/last40Days/{{stationId}}
Authorization: Bearer {{auth_token}}
